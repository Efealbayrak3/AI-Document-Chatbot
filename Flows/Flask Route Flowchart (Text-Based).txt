        ┌────────────────────┐
        │   User visits "/"  │
        └────────┬───────────┘
                 │ (GET)
                 ▼
        ┌────────────────────────────┐
        │ Render chatbot UI (form)   │
        │   index.html               │
        └────────┬───────────────────┘
                 │
        User types a query + submits (POST)
                 │
                 ▼
        ┌────────────────────────────┐
        │    Route: "/" (POST)       │
        └────────┬───────────────────┘
                 ▼
        ┌────────────────────────────┐
        │ ai_chatbot_response(query) │
        │ ┌─> fuzzy match            │
        │ └─> if needed, call Hugging Face AI
        └────────┬───────────────────┘
                 ▼
        ┌────────────────────────────┐
        │ Show result: link to PDF   │
        │  index.html with response  │
        └────────┬───────────────────┘
                 │
          User clicks link
                 │
                 ▼
      ┌───────────────────────────────┐
      │ Route: /download/<filename>   │
      │ └──> Send the file as download│
      └───────────────────────────────┘
